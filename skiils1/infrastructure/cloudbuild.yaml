steps:
  # Build and Push using Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-northeast3-docker.pkg.dev/$PROJECT_ID/app-repo/$_SERVICE_NAME', '.']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-northeast3-docker.pkg.dev/$PROJECT_ID/app-repo/$_SERVICE_NAME']
  
  # Deploy to Cloud Run with Port Handling
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '$_SERVICE_NAME'
      - '--image'
      - 'asia-northeast3-docker.pkg.dev/$PROJECT_ID/app-repo/$_SERVICE_NAME'
      - '--region'
      - 'asia-northeast3'
      - '--allow-unauthenticated'
